[project]
name = "logsentinel"
version = "0.1.0"
description = "Log Analyze"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
